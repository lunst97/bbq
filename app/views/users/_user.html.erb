<span class="user">
  <span class="user-avatar">
    <small><%= image_tag(user_avatar(user), class: 'image-icon-small') %></small>
  </span>
  <span class="user-name">
    <% if request.path == user_path(user) %>
      <%= user.name %>
    <% else %>
      <%= link_to user.name, user_path(user) %>
    <% end %>
  </span>
</span>